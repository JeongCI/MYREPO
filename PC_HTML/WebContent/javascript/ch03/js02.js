/**
 * 
 */
 
 'use strict';
 
 const doAdd = (s1, s2) => s1+s2;
 console.log(`(s1, s2) => s1+s2 : ${doAdd(11,13)}`);
 
 const doMulti = (s1, s2) => {
  let mul = s1*s2;
  return mul;
};
  console.log(`doMulti(2,3): ${doMulti(2,3)}`);